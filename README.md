# React Hooks Demo

A minimal Vite + React project that demonstrates:

- useState
- useEffect
- useMemo
- useRef
- custom hooks via `useLocalStorage`

Source files use the `.jsx` extension throughout the React app.
Each hook example is split into its own component inside `src/components`.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
